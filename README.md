# Telegram Auto-Joiner - Инструкция по использованию

## ⚠️ ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ

### Юридические и этические аспекты:
1. **Соблюдайте ToS Telegram** - массовые автоматические действия могут нарушать условия использования
2. **Риск бана** - Telegram может заблокировать аккаунты за подозрительную активность
3. **Используйте только для легальных целей** - управление своими каналами, тестирование, исследования
4. **FloodWait** - Telegram имеет строгие лимиты на действия, их превышение приведет к временной блокировке

### Рекомендации безопасности:
- Не используйте основные аккаунты для автоматизации
- Используйте качественные прокси
- Соблюдайте разумные интервалы между действиями
- Не присоединяйтесь к спам-каналам

---

## 📋 Требования

### Установка зависимостей:
```bash
pip install telethon python-socks[asyncio]
```

### Необходимые данные:
1. **API_ID и API_HASH** от Telegram:
   - Получите на https://my.telegram.org/apps
   - Замените в коде `YOUR_API_ID` и `YOUR_API_HASH`

2. **Session файлы (.session)**:
   - Должны находиться в той же папке, что и скрипт
   - Создаются при первой авторизации через Telethon

3. **Файл proxy.txt** (опционально):
   Формат: `login:password@ip:port`
   Пример:
   ```
   user346038:hjzuvp@194.147.6.92:19182
   user346038:hjzuvp@194.147.6.56:19182
   user12345:pass123@195.123.45.67:8080
   ```

4. **Файл groups.txt**:
   Список ссылок на каналы/группы (по одной на строку)
   Пример:
   ```
   https://t.me/pro_zdorovie_daryasemm
   https://t.me/kempel_yulia
   https://t.me/zhenya_prorab
   https://t.me/bezuderjnoe_veselie
   ```

---

## 🚀 Использование

### Подготовка:
1. Разместите все .session файлы в папке со скриптом
2. Создайте файл `groups.txt` со ссылками на каналы
3. Создайте файл `proxy.txt` с прокси (если нужно)
4. Укажите ваши API_ID и API_HASH в коде

### Запуск:
```bash
python telegram_auto_joiner.py
```

---

## ⚙️ Как работает программа

1. **Загрузка данных**:
   - Сканирует папку на наличие .session файлов
   - Загружает список групп из groups.txt
   - Загружает список прокси из proxy.txt

2. **Подключение**:
   - Создает отдельное подключение для каждой сессии
   - Распределяет прокси между сессиями (циклически)
   - Все сессии работают параллельно

3. **Вступление в группы**:
   - Каждая сессия вступает в 2 группы в час (настраивается)
   - Добавляется случайная задержка для имитации человеческого поведения
   - Автоматически пытается вступить в обсуждения каналов

4. **Обработка ошибок**:
   - ChannelsTooMuchError - превышен лимит каналов
   - ChannelPrivateError - канал приватный
   - UserAlreadyParticipantError - уже в канале
   - FloodWaitError - нужно подождать

---

## 🔧 Настройка параметров

В коде можно изменить:

```python
# Количество вступлений в час на одну сессию
JOINS_PER_HOUR = 2

# Задержка рассчитывается автоматически:
# DELAY_BETWEEN_JOINS = 3600 / JOINS_PER_HOUR  # секунды
```

Для более агрессивного режима (рискованно):
```python
JOINS_PER_HOUR = 5  # Увеличит скорость, но повысит риск бана
```

Для более безопасного режима:
```python
JOINS_PER_HOUR = 1  # Медленнее, но безопаснее
```

---

## 📝 Создание session файлов

Если у вас еще нет .session файлов, создайте их отдельным скриптом:

```python
from telethon import TelegramClient

API_ID = 'YOUR_API_ID'
API_HASH = 'YOUR_API_HASH'

async def create_session():
    session_name = input("Введите имя сессии: ")
    client = TelegramClient(session_name, API_ID, API_HASH)
    await client.start()
    me = await client.get_me()
    print(f"Авторизован как: {me.first_name}")
    await client.disconnect()

import asyncio
asyncio.run(create_session())
```

---

## 🐛 Устранение неполадок

### "Не найдено .session файлов"
- Убедитесь, что .session файлы находятся в той же папке
- Проверьте расширение файлов

### "Сессия не авторизована"
- Пересоздайте session файл через авторизацию
- Проверьте API_ID и API_HASH

### Ошибки прокси
- Проверьте формат в proxy.txt
- Убедитесь, что прокси работают (протестируйте отдельно)
- Используйте только SOCKS5 прокси

### FloodWaitError
- Программа автоматически обрабатывает эту ошибку
- Уменьшите JOINS_PER_HOUR
- Используйте больше разных аккаунтов

---

## 📊 Пример вывода программы

```
============================================================
Telegram Auto-Joiner
============================================================
Загружено 3 прокси
Загружено 10 групп для вступления
Найдено 5 сессий: ['session1', 'session2', 'session3', 'session4', 'session5']

============================================================
Запуск 5 сессий параллельно...
============================================================

[session1] Подключение через прокси: 194.147.6.92:19182
[session2] Подключение через прокси: 194.147.6.56:19182
[session1] ✓ Подключен: Ivan (79991234567)
[session1] Попытка вступить в: pro_zdorovie_daryasemm
[session1] ✓ Вступил в pro_zdorovie_daryasemm
[session1] ✓ Вступил в обсуждение канала pro_zdorovie_daryasemm
[session1] Ожидание 29.5 минут до следующего вступления...
```

---

## 🔒 Безопасность

1. **Не делитесь .session файлами** - они дают полный доступ к аккаунту
2. **Храните API_ID и API_HASH в секрете**
3. **Используйте .gitignore** если храните код в репозитории:
   ```
   *.session
   *.session-journal
   proxy.txt
   groups.txt
   ```

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте все предупреждения выше
2. Убедитесь в правильности формата файлов
3. Проверьте логи программы на наличие конкретных ошибок

---

**Помните**: Автоматизация действий в Telegram - это зона риска. Используйте с умом! 🧠
